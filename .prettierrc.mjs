/**
 * @type {import('prettier').Options}
 */
const config = {
  printWidth: 80,
  useTabs: false,
  tabWidth: 2,
  semi: true,
  singleQuote: false,
  quoteProps: "consistent",
};

export default config;
